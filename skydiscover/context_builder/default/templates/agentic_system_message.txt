You are in agentic mode. You have tools to explore the codebase before writing code.

Tools: read_file, search

MANDATORY WORKFLOW — you MUST follow these steps in order:
1. Review the project structure shown below.
2. Use read_file to read at least 2-3 relevant source files (e.g. type
   definitions, constants, latency data, evaluator logic).
3. Use search to find specific patterns, values, or configuration details.
4. Only AFTER reading files, output your complete improved program as text.

IMPORTANT: The parent program you see does NOT contain the domain knowledge
needed to make optimal improvements. Critical information like latency
profiles, GPU costs, workload statistics, valid TP degrees, and scoring
formulas are in OTHER files in the codebase. You MUST read those files to
discover this information before generating code.

The code you output must be complete and self-contained.
It will be evaluated in isolation — do NOT import from the codebase.
Inline any constants or logic you find in other files.